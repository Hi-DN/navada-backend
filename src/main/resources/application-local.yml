spring:
  datasource:
    url: jdbc:log4jdbc:mysql://localhost:3306/navada?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: root
    password: kalswl99

  jpa:
    hibernate:
      ddl-auto: create
      show-ddl: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
  messages:
    basename: i18n/exception
    encoding: UTF-8